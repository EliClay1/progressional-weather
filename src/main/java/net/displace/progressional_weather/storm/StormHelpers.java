package net.displace.progressional_weather.storm;

public class StormHelpers {

/*
* var multiplier = stormData.properties().get("mob_spawn_multiplier");
// multiplier is a Dynamic<?>, not the original value

// To use it:
if (multiplier instanceof Dynamic<?> dynamic) {
    float value = dynamic.asNumber()
        .result()
        .orElse(1.0f)
        .floatValue();
}
*
*
*
* */

}
